/** @type {import('next').NextConfig} */

const nextConfig = {
    images:{
        remotePatterns:[
            {protocol:'http', hostname:'146.190.140.32', port:''}
        ]
    }
}

module.exports = nextConfig
